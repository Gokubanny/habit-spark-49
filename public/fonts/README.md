# Hago font

Drop your Hago files here:

- `Hago.woff2`        (regular — weights 400–600)
- `Hago-Bold.woff2`   (bold — weights 700–900)

They are auto-loaded by `@font-face` in `src/styles.css`.
Until you upload them, the app falls back to Manrope, which is visually similar.
